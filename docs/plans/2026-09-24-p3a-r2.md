# P3a round 2: amendments; continue from step 2

Continue the P3a plan (`docs/plans/2026-09-24-p3a-bindings-in-db.md`, the plan this binding
received as round 1) from where round 1 stopped. Round 1's step 1 files stay as they are.
Everything in that plan still holds, **except the amendments below**. The stop rule is unchanged.

## Amendments

**A1: the version-pinned tests are sanctioned.**

Port these assertions from the literal `1` to the embedded maximum migration version:
- `internal/db/db_test.go` :53, :85, :93 (row count = the number of embedded migrations), :115, :209
  (every embedded version appears exactly once);
- `internal/db/read_test.go` :592.

Derive the expected value from the embedded migration files, using the unexported `maxEmbedded`
(`internal/db/migrate.go:70`) or the embedded file list. **Never hard-code 3**: another branch
adds migration 002, and these tests must stay right when it lands.

**A2: fork saves first.**

`internal/relevo/fork.go` `writeFork` (lines 383-414) is added to the plan's file list. Its body
becomes, in this order:

```
tx.ForkState(src, b.Name, throughRound)   // unchanged: writes dst/NNN-* and dst/log.jsonl (§4.6)
tx.Save(b)                                // RecordPut, then importPresent(dst) adopts dst/log.jsonl
tx.AppendLog(b.Name, forkEntry)           // Seq = max+1 after the adopted entries
tx.AppendLog(b.Name, pick)
success = true
```

The failure `defer` becomes `if !success { _ = tx.Delete(b.Name); _ = os.RemoveAll(s.Dir(b.Name)) }`,
so a failed fork leaves neither a record nor a dir. Update writeFork's doc comment to say so.

The final log order (the forked entries, then fork, then pick) and the final binding are the same
as today. **If an existing fork test asserts on Seq values or entry order and fails, stop and
report**; do not adjust it.

§2's table line for `internal/store/fork.go` is superseded by §4.6. ForkState keeps writing
`dst/log.jsonl` as a file, and it writes no record.

**A3: owner matching.** Round 1's `RecordGet` empty-owner wildcard (an owner of "" matches a live
row of any owner) is accepted. Store roots are single-owner until phase 5.

**A4: steps 2 and 3 are one step.**

Implement §4.2-§4.6 together and then run the step-2 test and the step-3 mutation check.

The sanctioned test list in §8 also gains:
- `internal/relevo/fork_test.go` and `internal/store/fork_test.go`: only for the order change in A2,
  and only if they call writeFork/ForkState in a way that now needs a Save first;
- the §A1 lines.

## Working efficiently

As in the plan's §7. Start with `git status` to see round 1's three untracked files, then read the
§7 batch.

## Steps

1. A1: port the six assertions. Verify: `go test ./internal/db/ -count=1` is green.
2. §4.2-§4.6 + A2 (plan steps 2+3 merged). Verify: the focused loop, the step-2 test, and the
   mutation check.
3. Plan step 4 (StoreSource, ListFiles).
4. Plan step 5 (full check and report, including every item the plan's step 5 lists).
