# Wave 2 tidy: five loose ends builders flagged today (docs nesting, stop label, serve usage, [ ] hint, opencode 2.x guidance)

Five small, independent fixes in one round. This plan stands alone. If a
step is impossible as written or contradicts the code, **halt and
report**. Foreground commands only; no sub-agents; never `make check`
here (gate commands below); no git fetch/rebase.

## 1. Items

1. **README `## Gate` nesting.** The `### Repair rounds` subsection (#132
   part 2) was inserted mid-section, so the general gate paragraphs that
   follow it ("While the gate runs…", "Configure it with `--gate`…", the
   log format, `relay status`) now read as sub-content of "Repair rounds".
   Move `### Repair rounds` (heading through its last paragraph, unchanged)
   to the end of `## Gate`, just before the next `##` heading. Content
   identical; only position changes.
2. **`stopping` after an abandon.** `internal/relay/status.go` shows
   `stopping <elapsed> of <grace>` whenever `StopRequestedAt` is set and the
   round is open -- including after the grace elapsed and the binding went
   NEEDS YOU (`stopped/abandoned`), where it reads `stopping 7m of 5m0s`.
   Show it only while `stopDecision(b, now) == stopWait`; after the grace
   the NEEDS YOU line already says why. Test: extend `TestStatusShowsStopping`
   (or its neighbour) with a `StopRequestedAt` older than the grace on a
   `needs_you` binding -> `BuilderStatus` does not start with `stopping`.
3. **Top-level usage lists the serve verbs.** `cmd/relay/main.go`'s
   `usage` const line for `serve` names `init|enroll|clients|revoke|fingerprint|status|gc|unbind`;
   add `ui|gates|available|unavailable` (the sub-usage in `cmd/relay/serve.go`
   already has them). Test: `TestServeUsageOnNoArgs` or a `relay help`
   capture asserts `gates` appears in the top-level text (store-only;
   reaches no herdr).
4. **`relay ui` footer advertises `[ ]`.** `internal/ui/model.go`
   `footerView` key hints gain `[ ] round` next to the tab hints. Extend
   the footer test in `split_test.go` (`TestFooterNoticesAndRefreshAge` or
   the closest) to assert it; regenerate goldens only if one changes and
   say so.
5. **README: opencode 2.x shared service (#256).** In the headless
   builders section add a short "opencode 2.x" note: one
   `opencode serve --service` per user hosts every `opencode run`; keep
   concurrent opencode headless builders on one machine to two or three;
   after a switch away from an opencode 2.x builder, check the worktree
   for a second writer before re-sending (`git status`, file mtimes); link
   #256. Also `relay doctor`'s opencode row prints the opencode major
   version if `BinaryVersion` is already collected for it (look at
   `internal/doctor/doctor.go`'s version row; if the version is already
   shown, this half is a no-op -- say so).

## 2. Files

```
README.md, internal/relay/status.go, internal/relay/status_test.go,
cmd/relay/main.go, cmd/relay/serve_test.go (or main_test.go),
internal/ui/model.go, internal/ui/split_test.go (+ goldens if changed),
internal/doctor/doctor.go (only if item 5's second half applies)
```

## 3. Steps

One task per item, in the order above; tests first where an item names
one. Gate at the end, in the foreground, stop at the first failure:
```
test -z "$(gofmt -l .)" || { gofmt -l .; exit 1; }
go vet ./...
go test -count=1 ./...
go mod tidy && git diff --exit-code go.mod go.sum
```
Commits: one `fix(...)` per item is fine (five commits), or one
`fix(tidy): ...` for all; never `feat:`. Copy this plan to
`docs/plans/2026-09-21-w2t1-tidy.md` in a `chore(plans):` commit.

## Report

Per item: done / no-op (and why), test names, then the gate tail and the
commit shas.
