# #303 (drop herdr): handoff, 2026-09-22 evening

Spec: `docs/specs/2026-09-22-drop-herdr-design.md` (merged in #320).
Builders are remote on contabo (cline-pass/deepseek, picked by the order).

## Waves

| wave | step | plan | binding | state at handoff |
|---|---|---|---|---|
| 1 | 1a planner registry + `relay planner` + hook | `w303a-planner-registry.md`, round 2 `w303a-r2-legacy-ids.md` | `h-reg` | verified by the planner (`make check`, hook live-tested against a copy of the real relay.db); PR #321 |
| 1 | 2 headless-only builders | `w303b-headless-only-builders.md` | `h-hless` | **round 1 running on contabo** |
| 2 | 1b wiring (bind/mcp/claims/doctor) | `w303c-planner-wiring.md` | -- | written, not sent. Send after #321 **and** step 2 merge |
| 2 | 2b headless-only consults, `reap` goes | `w303d-headless-only-consults.md` | -- | written, not sent. Same gate, in parallel with 1b |
| 3 | 3 delivery without herdr, delete `internal/herdr` | not written | -- | after 1b + 2b |
| 4 | 4 non-Go (packaging, docs, agent defs) and 5 headless e2e in CI | not written | -- | after 3, in parallel |

## Collecting `h-hless` from another machine

The round runs on contabo whether or not the dispatching machine is up.
Its worktree on the server is:

```
/srv/data/relay-serve/serve/bindings/54b11114c667bcde474b0fc6081afa1573c482ce107807dd53de4152130ca88d/.worktrees/h-hless
```

The branch is `relay/h-hless`. The report, once the round closes, is
`/srv/data/relay-serve/serve/bindings/54b11114c667bcde474b0fc6081afa1573c482ce107807dd53de4152130ca88d/h-hless/001-report.md`
(`ssh contabo cat …`). Tested from the laptop on 2026-09-22: `ssh contabo`
logs in as `fuad`, and `git ls-remote` on the URL below lists `relay/h-hless`.
From the laptop:

```
git fetch ssh://contabo/srv/data/relay-serve/serve/bindings/54b11114c667bcde474b0fc6081afa1573c482ce107807dd53de4152130ca88d/.worktrees/h-hless relay/h-hless:refs/remotes/contabo/h-hless
```

Then verify it as usual: a detached worktree, `make check`, `make e2e` (the
round changed reconcile.go), `go vet -tags e2e ./internal/relay/`, and a
diff check against the plan's keep-list, since planner-side pane delivery
must survive. Then open a PR.

The laptop's relay state doesn't know `h-reg` or `h-hless`. They were
bound from the desktop's pane `wG:p1A`. Send later waves from new bindings
made on the laptop.

## Verification notes so far

- 1a: legacy `planner.id`s are ULIDs (`01M3…`), and round 2 made
  `ValidID` accept them. The hook keeps one id across startup → clear when
  the parent process is the same (the real hook's parent is the Claude
  process).
- Live check still owed after 1a merges: update the relay plugin, open a
  fresh Claude session, and check `RELAY_PLANNER` plus the "You are relay
  planner" context; then `/clear`, prompt once, and confirm the same id.
  That's step 1b's done-criterion in the spec.

## Coordination

- #293 part 2 is held by the human until step 4.
- #123 (pQ's planner-commands spec, branch `docs/planner-commands-spec`)
  is held behind #303. It adds `Claim.Mode` and two doctor rows beside
  1b's `plugin` row.
- Spike branches pushed for reference: `spike/303-go` and
  `spike/303-nongo` (base `15e2b74`).
