# #303 step 4: everything that isn't Go (plus release provenance)

This plan stands alone: everything you need is in this file and in the tree.
The design is `docs/specs/2026-09-22-drop-herdr-design.md` (§2, and §7
step 4). Steps 1–3 have merged: relay's Go code no longer references herdr.
If a step is impossible as written or contradicts the code, **halt and
report**. Do not improvise around it.

**Work directly: do not dispatch sub-agents or explore agents.**

## Working efficiently (read this before step 1)

Your provider has about **3 s of round-trip latency per model step**,
whatever the step does. Generation itself is fast. The last comparable
round took 307 steps, and 261 of them made a single tool call. The cost
of this round is its **number of steps**, so:

1. **Batch independent tool calls in one step.** When you need several
   files, ranges or searches that don't depend on each other, issue them
   all as parallel tool calls in the same response. The same goes for
   independent edits to different files.
2. **Read once, from the locations given.** This plan names files,
   functions and line ranges. Read each file you will edit once (the named
   ranges, or the whole file if it's under ~400 lines) before editing it.
   Don't grep for things the plan already located, and don't re-read a
   range you have already seen unless you edited it.
3. **Edit in as few calls as possible.** Make every change to a file in one
   edit call (several hunks), or rewrite the file when most of it changes.
   Prefer one scripted edit (a short `python3` or `perl` over named ranges)
   to many single-hunk edits when the change is mechanical.
4. **One tight build/test loop.**
   - Iterate with `go build ./... 2>&1 | head -80` and focused
     `go test ./<pkg> -run '<names>'`.
   - Fix **every** error a run reports before running again.
   - Run `make check` once when you believe you are done, and once more
     only if it fails.
5. Don't run `git status`/`git diff` between edits for reassurance. Run
   them at the start and before the commit.

Another builder is writing a headless e2e test (`internal/e2e/headless_test.go`)
in parallel. Don't touch `internal/e2e`.

## Done means

`grep -rli herdr . --exclude-dir=.git` lists only:
- files under `docs/specs/`, `docs/plans/` and `docs/superpowers/` (history:
  leave them alone), and
- any line of the new README/CLAUDE.md that says, in one sentence, that
  relay no longer integrates with herdr.

`make check` passes.

## 1. Delete

- `herdr-plugin.toml`, `from-source/herdr-plugin.toml` (and `from-source/`
  if it ends up empty);
- `scripts/plugin-build.sh`, `plugin-daemon-check.sh`, `plugin-fetch.sh`,
  `plugin-install-service.sh`, `plugin-open-pane.sh`, and their `_test.sh`
  files.

## 2. Edit

- **`scripts/check-plugin-version.sh`** and its test: it checks only the
  two Claude Code manifests, `claude-plugin/.claude-plugin/plugin.json` and
  `.claude-plugin/marketplace.json`. It keeps the tag check and the
  10-`feat` drift guard exactly as they are.
- **`Makefile`**:
  - `release` stops bumping the herdr manifests, and the "bump before tag"
    rule text that exists only because of them goes.
  - `e2e`: step 5 replaces the test. Make the `e2e` target run `go test
    ./internal/e2e/ -run TestHeadlessE2E -count=1`, and drop any herdr
    session setup.
  - Nothing references a deleted script. Grep for them.
- **`.github/workflows/release.yml`**: drop the herdr-manifest steps. The
  tag check and the release archives are unchanged.
- **`internal/release/provenance.go`** (+ test): delete `KindPluginRelease`
  and `KindPluginSource` and the `ManifestVersion` input. A binary that
  sits next to an old `herdr-plugin.toml` now reads as the kind it
  otherwise is, `go-install` / `local-build` / `unknown`. Update every
  caller and the staleness text that named the plugin variants.
- **`README.md`**: remove the herdr plugin install, "Panes are yours",
  "Where the builder appears", the anti-clobber / HELD / nudge / scrape
  text, `relay answer`, `relay reap`, `--headless` as a choice (it's the
  only mode), `--held-grace`, `stop --grace/--now`, pane adoption, and the
  herdr sidebar tokens.
  - Add the install path: release tarball, `go install` or `make install`,
    then `make service`; then the Claude Code plugin `/plugin install
    relay@relay` (the MCP channel and the `SessionStart` hook that runs
    `relay planner init`).
  - Explain in 3–6 lines that a planner session is identified by relay
    (`relay planner list`), and that reports arrive over the channel, a
    deliverer (opencode), or `relay wait`/`relay pull`.
- **`CONTRIBUTING.md`**: drop herdr prerequisites and the herdr e2e
  instructions; `make e2e` runs in CI now.
- **`CLAUDE.md`** (repo root):
  - Remove the pane-closing rules (`relay reap`, the mid-round switch
    closing a pane, `relay pause` closing a pane, "close the orphaned
    builder pane yourself").
  - Rewrite the `make e2e` paragraph: it's headless and CI runs it.
  - Keep every other rule, including the CI and `cmd/relay` test
    constraints, rewording "a subcommand that reaches herdr" to "a
    subcommand that spawns a harness or reaches the network".
- **`internal/harness/agents/architect.*.md`** and **`reviewer.*.md`**
  ("Handing off", and the reviewer's "own pane" / findings-file lines):
  - no pane mode;
  - no `relay answer`;
  - no `--headless` advice (it's the only mode);
  - the planner is identified by relay (`RELAY_PLANNER`, set by the
    plugin hook).
  
  Keep the rest of each definition byte-for-byte. Update
  `TestArchitectHandoffIsSharedAcrossKinds` (internal/harness) to pin
  text that still exists: it must still fail if the definitions diverge
  across kinds.
- **`.github/ISSUE_TEMPLATE/*`**: drop herdr version fields and
  references.
- **`dist/*`**: comments only. Keep `scripts/relay-service-template_test.sh`
  passing.
- `docs/design.md` calls itself a historical record: add one line at the
  top, "Superseded in part by `docs/specs/2026-09-22-drop-herdr-design.md`
  (#303)", and change nothing else.

## 3. Verification before you report

- `make check` passes, including the shell script tests.
- The grep under "Done means": paste its output.
- Report: files deleted, files edited, and every departure with its reason.
- Commit with `docs: …` for the prose and `refactor(release): …` for
  provenance. Two commits are fine. Don't rebase and don't push.
