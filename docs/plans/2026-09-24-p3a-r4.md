# P3a round 4: the migrate fixture, name keys everywhere, then the full check and commit

Continue P3a from round 3's uncommitted state. The stop rule is unchanged.

**C1: sanctioned fixture change in `internal/migrate/migrate_test.go`.** `saveBinding` (~:561)
seeds what stands for an old, **pre-DB** relay root, yet it uses `store.New(root).Save`, which is
now DB-backed. Change it to write the pre-DB layout directly:
`<root>/<name>/bind.json` as `json.MarshalIndent(b, "", "  ")`, the way a pre-P3a store wrote it.
Create the dir first, with mode 0755 and file mode 0644. The four tests
(`TestRunHappyPath`, `TestRunRefusalsAllAtOnce`, `TestRunWarnings/failed_repair`,
`TestRunOrphanNotRepairedTwice`) must then pass **with no other edit**.
- If a test also reads back through `store.New(root).Load/List` on the old or new root, stop and
  report which one.
- If `saveBinding` is also used for a *new* (post-migration) root, keep a DB-backed variant for
  those call sites and say which ones.

**C2: B2 applies to every record writer.** `RecordArchive`, `RecordDelete` and `RecordSetViewed`
key on the live row by **name** alone, as `RecordPut` and `RecordGet` do. Drop their owner
parameters and update the callers and `record_test.go`.

## Steps

1. C1, C2. Verify with the focused loop from the plan's §7.
2. `make check` and `make e2e`. Report the items the plan's step 5 lists.
3. **Commit** everything as one commit on `relevo/p3a`. Do not rebase or merge.
