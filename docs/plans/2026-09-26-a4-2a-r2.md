# A4-2a, round 2 (local; the remote copy halted because the server repo has no origin/main): format 8, a quiet text status, the real mutation, exact fixture keys

Round 1 was right on every point it flagged. This round fixes them. Same spec and
vocabulary, a clean break (D1), and internal Go names kept (D3).

**If a step is impossible as written or contradicts the code, stop and report. Do not
improvise, and do not bend a test to fit.**

## 1. BindingFormat 8

#596 already took format 7 (`oom_requeue`), so a binary built between #596 and A4 knows
7 and would load an A4 record and erase its renamed keys on save.
- `store.BindingFormat` becomes **8**, and `recordFormat` returns 8.
- The format.go comment says why 8 without history: every record now carries the
  runner, candidate and actor keys, so any binary that predates them must refuse it.
- Rename the round-1 tests `TestSaveWritesFormat7…` to `…Format8…`, with their
  assertions.
- Regenerate `internal/store/testdata/binding-shape.golden` with the **sanctioned**
  `go test ./internal/store -run TestBindingShapeMatchesFormat -update`. It now runs,
  because the format is bumped. Do not compute it by hand.

## 2. The text status stays quiet for the builder

`internal/view/render.go:~149` prints ` actor <r>` when `b.Role != ""`. Since the role is
now always set, every row ends in "actor builder". Print it only when
`b.Role != "builder"`, which is today's behaviour for builder rows.
- Port the writer-role test back: a builder row prints no actor suffix, and a designer
  row prints `actor designer`.
- The JSON keeps `"actor"` always present; only the text changes.

## 3. The real mutation for "actor always present"

Round 1's mutation (c) could not fail, because it was the wrong mutation. Run this one
instead: remove the `bindingRole` fallback in `internal/relevo/status.go` (the status
row's `Role` from `b.Role` as stored, with no "builder" default).
- `TestStatusDocumentAlwaysNamesTheActor` must fail. If it does not, strengthen the test
  so it uses a record whose stored role is empty, and say so.
- Restore it after.

## 4. Round writers never store an empty actor

`internal/db/write.go` `UpsertRound` writes `actor=?` from `Round.Actor`, so a caller
that leaves it empty stores `""`. `cmd/relevo/contract_test.go` `seedHistoryFixture`
does, which is why `history-*.golden` shows `"Actor":""`. Ingest always sets it.
- In `UpsertRound`, an empty `Actor` is written as `"builder"`, the column's own
  default and the seeded actor, with a one-line comment.
- Add `TestUpsertRoundEmptyActorIsBuilder` in internal/db.
- Regenerate `cmd/relevo/testdata/contract/history-*.golden`. `"Actor"` becomes
  `"builder"`; list them.

## 5. Exact fixture keys

`#600` is merged. `db.RoundRow`'s JSON keys are its Go field names:
`BindingID, BindingName, Repo, Feature, Number, StartedAt, ClosedAt, Outcome, Actor, Candidate, Harness, Provider, Model, Commits, Tree, GateResult, CostUSD, CostBasis, InTokens, CacheTokens, WriteTokens, OutTokens, ReportOutcome, Mode, Server, DurationMS, Switches, Archived, ArchivedAt`.
- Make `scripts/testdata/opencode-plugin/history.json` use exactly these, matching
  `history-binding.golden`'s shape, with `"Actor":"builder"`.
- **This branch's base predates #600.** First run `git fetch origin` and
  `git merge --no-ff origin/main` (expect no conflicts; the planner checked
  `merge-tree`), so the keys exist here. If there is a conflict, stop and report.

## 6. docs/design.md

Its rows naming `to_builder` and `builder_candidate` (and any other renamed key from
spec §3.2 and §3.3) use the new names. Leave historical `docs/plans/*` alone.

## Checks (this laptop cannot take race suites; do NOT run make check, go test ./... or -race)

- `gofmt -l $(git ls-files '*.go')` prints nothing
- `go build ./... && go vet ./internal/store/ ./internal/view/ ./internal/db/ ./internal/relevo/ ./cmd/relevo/`
- `sh scripts/check-comments.sh && sh scripts/check-filesize.sh && sh scripts/check-plugin-version.sh`
- `golangci-lint run ./internal/store/... ./internal/view/... ./internal/db/... ./internal/relevo/... ./cmd/relevo/...`
- `go test -count=1 ./internal/store/ ./internal/view/ ./internal/db/ ./internal/ingest/ ./internal/histq/ ./internal/relevo/ ./internal/serve/ ./internal/mcp/ ./cmd/relevo/`

## Commit

The merge commit from §5, then **one** new commit:
`fix(a4): record format 8, quiet builder rows, round actor never empty`.
Never amend, never rebase, and never force-push. This round needs no plan copy; the
owner adds it.

## Report

The report covers:
- the new mutation's result;
- the goldens that moved;
- the merge result;
- `make check`'s last lines.
