# A5 R5b, round 2: the reader round uses the writer's compact header

Owner decision: a reader round's header matches the **writer** round's compact two-line
layout, which is what the cockpit actually has (see `internal/ui/testdata/round-real-132.golden`).
It does not use the boxed card from round 1: that board came from an older design that
was never built. Only the reader-specific facts change.

**If a step is impossible as written or contradicts the code, stop and report. Do not
improvise, and do not bend a test to fit.**

## 1. The header

- Remove the reader-only boxed card that round 1 added (the `╭─ … ╮` box, its state
  chip line, its token line, and its `s send next / r retry on…` key row).
- Render the reader round through the **same** header code path as writers, with these
  substitutions:
  - **Line 1, left:** the state and time as writers show them, followed by
    `· N artifacts · <total size>`, and `· repository unchanged` once the round is closed.
  - **Line 1, middle:** `<actor> on <candidate> · reader · <planner> · scratch @ <short baseline head> + dirty diff`.
    Writers show `<candidate> · <planner> · <branch>`; follow their spacing and styles.
  - **Line 1, right:** `round i of n`, as writers.
  - **Line 2:** the tokens line, exactly as writers render it. Whatever writers show for
    the price, readers show too; do not special-case it.
- Keys are only in the footer, as for writers. Reader rounds show no `g gate`.
- Remove the duplicate `N artifacts · size` line that round 1 put above the artifacts
  table. The count is in the header and in the tab label.

The target shape, at 132 columns, for a closed reader round with the artifacts tab open:

```
  ◆ relevo   fleet › review-568 ›  r1                                                          v…    14:02
                                                                                                    
    reported   9m · 2 artifacts · 12.4k · repository unchanged      reviewer on gpt-5.6-terra · reader · architect-5 · scratch @ 043cf35 + dirty diff      round 1 of 1
   tokens in 212k · cache 1.9M (90%) · out 18k · …

   plan     artifacts 2     log     transcript                                              round   [   r1 of 1   ]

     FILE                          SIZE    WRITTEN   OPENS IN
     summary.md                    1.1k    14:00     pager
     findings.md                   11.3k   14:00     pager

     summary.md · the reviewer's final message
     …
```

If line 1 is too long at 132 or 100 columns, apply **the writers' own truncation rule**,
whatever it is. Say which parts drop first.

## 2. Tests

- Regenerate only `round-reader-artifacts-132` and `round-reader-artifacts-100`. Every
  writer golden must pass unchanged. Report both goldens in full.
- **Required mutation.** Render a reader round through a separate header function that
  does not share the writer path (or re-add the card), and confirm
  `TestReaderHeaderSharesTheWriterLayout` fails; then restore. The test asserts that
  line 3 of a reader golden starts with the same state-and-time cell layout as a writer
  golden with the same state.
- Before committing, run `grep -n '§\|#[0-9]' <every file you touched>`: it must print
  nothing in comments.

## 3. Checks, on this server

- `go build ./... && go test -count=1 ./internal/ui/`
- `export TMPDIR=$HOME/.cache/go-tmp GOTMPDIR=$HOME/.cache/go-tmp; mkdir -p $TMPDIR; make check`

## 4. Commit

Commit as **one new commit**: `fix(a5): the reader round uses the writer's compact header`.
Never amend, and never rebase. This round needs no plan copy; the owner adds it.

## Report

The report covers:
- both goldens;
- the truncation finding;
- the mutation;
- `make check`'s last lines.
