# P3a round 6: verify the rebased branch

The planner rebased `relevo/p3a` onto `main`, which now includes P2a (config in the DB,
migration 002). It reworded the `migrate_test.go:562` comment so the name guard passes, and took
`main`'s `internal/db` version-pin tests (the `embeddedVersion` helper). The branch is one commit,
`9cd139d`.

**Make no code change.** If anything fails, report it verbatim and stop.

## Steps

1. `git status` must be clean at `9cd139d`. Otherwise, stop.
2. `make check 2>&1 | tail -40`, then `make e2e 2>&1 | tail -10`. Paste both verbatim, with
   their exit codes.
3. If either fails: for each failing test, give its name, the first failure line, and whether
   the failure involves both P2a's config DB (`internal/config`, `newRuntime`) and P3a's store DB
   (`internal/store/db.go`), or just one of them.
