# HK1 round 2: re-run the full check

The planner has resolved round 1's blocker by dropping the `/relay` line from `.gitignore`.
`.gitignore` is now unchanged from HEAD, and step 5 of round 1 is withdrawn. Make no code changes
in this round.

**Stop rule:** if anything fails, do not fix it. Report the failure verbatim and stop.

## Working efficiently

One step: run the command below from the worktree root and report.

## Steps

1. Run `git diff --stat -- .gitignore`. Expect empty output. If it is not empty, stop.
2. Run `make check 2>&1 | tail -40`. Paste the output verbatim in the report.
3. Run `git diff --stat | tail -3`. Paste it verbatim.

Deliverable: the report, with those three outputs and a pass/fail line.
