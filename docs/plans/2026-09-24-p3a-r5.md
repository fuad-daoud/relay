# P3a round 5: TestRunHappyPath, then the full check and commit

Continue from round 4's uncommitted state, keeping C1. The stop rule is unchanged.

**C3: sanctioned edit to `TestRunHappyPath`** (`internal/migrate/migrate_test.go`).
- Change the `legacy.DBFile` placeholder writes (`relay.db` and `relay.db-wal`) to **zero-byte**
  files. SQLite treats a zero-byte file as an empty database and an empty WAL as no WAL, so the
  renamed `newstate/relevo.db` opens and migrates, and `store.New(stateTo).Load("one")` imports the
  moved `bind.json` exactly as a real upgrade would.
- Keep the existence assertions at :89 unchanged.
- If `Load` still fails, use this fallback instead: replace the read-back at :94-101 with a decode
  of `stateTo/one/bind.json` from disk, checking the same fields. Say which one you used.

## Steps

1. C3. Verify: `go test ./internal/migrate/ -count=1` is green, and the focused loop is green.
2. `make check` and `make e2e`. Report the items the plan's step 5 lists.
3. **Commit** everything as one commit on `relevo/p3a`. Do not rebase or merge.
