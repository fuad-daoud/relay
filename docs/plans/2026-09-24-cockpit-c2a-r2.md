# Cockpit C2a, round 2: four fixes to round 1

Round 1 is on this branch as the commit that added `internal/stats`. It was verified;
these are the planner's corrections to its own plan's gaps. The plan and the spec are
appended below for context. **Do only these four fixes.**

**If a fix is impossible as written or contradicts the code, stop and report. Do not
improvise.** CI rules are unchanged: pure tests plus the in-process
`history --stats` test. Nothing spawns a harness.

## F1. `Totals` gets its own field for the unrecorded count

- **Now:** the round-1 plan gave the `(unrecorded)` round count no field, so round 1
  stored it in `Totals.Candidates` (`internal/stats/stats.go:59`, incremented at 189).
- **Change:**
  - Add `Unrecorded int` to `Totals`, the rounds whose `BuilderCandidate` is nil.
  - `Candidates` becomes the number of **distinct non-nil** `BuilderCandidate`
    tokens among the rows.
  - `renderScorecard` (`render.go:72`) prints `r.Totals.Unrecorded`.
- **Tests:** `TestScorecardUnrecordedAndKeep` asserts both fields. Update the golden
  only if it changes; it should not, because the printed number is the same.

## F2. The cost-known rule matches `histq`

- **Now:** `costKnown` (`stats.go:501`) requires a non-nil basis.
  `histq.costKnown` (`internal/histq/group.go:187-195`) treats a nil basis as known,
  as long as `CostUSD` is non-nil and the basis is not `unknown`.
- **Change:** use histq's rule, plus the plan exclusion:
  `CostUSD != nil && (CostBasis == nil || *CostBasis != "unknown") && !plan`.
  `history --stats` and the `:rounds` grid must never disagree on a total.
- **Tests:** add a case to `TestScorecardPlanAndCost` where a row has a cost and a
  nil basis. It counts as known.

## F3. Empty measures print `-`, not zero

- **Now:** a scorecard row with `Closed == 0` prints `0%`, `0%` and `0m`, and a group
  row with `Landed == 0` prints `RNDS/LAND 0.0`. Those read as real numbers.
- **Change, in `render.go`:**
  - DONE, HALT and MED print `-` when `s.Closed == 0`. MED also prints `-` when
    `s.MedianMS == 0` and no closed row has a duration.
  - The fields already exist, so add `HasMedian bool` to `ScoreRow` if it is needed
    to tell "no durations" apart from "zero".
  - RNDS/LAND prints `-` when `Landed == 0`.
- **Tests:** extend the golden fixture (or `TestRenderEmpty`'s neighbour) with one
  open-only candidate and one repo with nothing landed, and regenerate
  `testdata/report.golden` with `-update`. **Read it** and paste the diff of the golden
  into the report.

## F4. Stale flag help

`cmd/relevo/history.go:166`: the `--stats` flag's help becomes
`"per-candidate scorecard, spend per day, reliability, repos and outcomes (default window 30d; --since all for everything)"`.

## Mutation check

Revert F2 to the round-1 rule. The new nil-basis case must fail. Report it, then
revert.

## Working efficiently

Read `stats.go`, `render.go`, `stats_test.go` and `render_test.go` once. Make each
file's edits in one call. Iterate on
`go test ./internal/stats/... ./cmd/relevo/ -run 'History|Stats'`. Run `make check`
once at the end. Report `git diff --stat`, which must touch only `internal/stats/**`
and `cmd/relevo/history.go`.
